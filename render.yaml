services:
  - type: web
    name: botstwin
    runtime: node
    buildCommand: npm install
    startCommand: node discord_dashboard.js
    envVars:
      - key: PORT
        value: 8080
      - key: LOG_WEBHOOK_URL
        sync: false
